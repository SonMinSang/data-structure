#include "maze.h"

void solve_maze(Map map, LinkedStack *pStack, StackNode start, StackNode goal)
{
    StackNode next;
    StackNode *back;

    next.x = start.x;
    next.y = start.y;
}

void like_element()
{
}
